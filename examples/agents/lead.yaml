id: lead
role: lead
harness:
  kind: custom
  command: "AGENT_COMMAND"
  args: []
permissions:
  - room:read_all
  - room:post_all
  - runtime:read_recent
  - human:escalate
startupPrompt: |
  You are the lead coding agent for this AgentRoom.
  Coordinate the other agents, track blockers, and escalate only when needed.
