/**
 * `@agentroom/diorama-core` — the Diorama World Protocol + (later) reducer,
 * layout, commands, and source adapter. See `docs/diorama/GAME_BRIDGE.md`.
 *
 * F2 ships the protocol types (§4.2). F3 adds the event→world reducer; F5 adds
 * the LayoutStrategy / SkinMap / WorldCommands / WorldSource interfaces.
 */
export * from "./protocol.js";
