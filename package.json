{
  "name": "agentroom-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.0.0",
  "type": "module",
  "bin": {
    "agent-room": "./bin/agent-room",
    "agent-roomd": "./bin/agent-roomd",
    "agentroom-mcp": "./bin/agentroom-mcp"
  },
  "engines": {
    "node": ">=24 <27"
  },
  "scripts": {
    "agent-room": "./bin/agent-room",
    "agent-roomd": "./bin/agent-roomd",
    "agentroom": "pnpm --filter @agentroom/cli dev --",
    "build": "tsc -b",
    "clean": "pnpm -r --parallel clean && rm -rf coverage",
    "dev:daemon": "pnpm --filter @agentroom/daemon dev",
    "dev:cli": "pnpm --filter @agentroom/cli dev --",
    "dev:mcp": "pnpm --filter @agentroom/mcp-server dev",
    "dev:tui": "pnpm --filter @agentroom/tui dev --",
    "dev:setup:fresh": "./bin/agent-room dev-new-user --run",
    "docs:build": "pnpm --filter @agentroom/docs build",
    "docs:dev": "pnpm --filter @agentroom/docs dev",
    "docs:preview": "pnpm --filter @agentroom/docs preview",
    "test": "vitest run",
    "typecheck": "tsc -b && pnpm --filter @agentroom/mobile typecheck",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "prettier": "^3.6.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
